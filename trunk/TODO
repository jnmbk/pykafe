* use ts for translations
* use qrc subsystem for icons
* use QSettings, leave database alone
* use qt's sqlite backend
* complete all inline TODO's
* menus should be capable of doing anything
* add shortcut keys to all functions and buttons
* Make applications unique like in KUniqueApplication or QtSingleApplication
